
export default function Header({ title, headerImg }) {
    return (
        
        <div id="Header">
            <h2>{title}</h2>
            <img src={headerImg} alt="to be added" />
        </div>
        
    )
}
